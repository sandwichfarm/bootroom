---
phase: 05-diagnostics-doctor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - crates/bootroom/assets/qemu/qemu-wasm-rev.txt
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "A committed sentinel `crates/bootroom/assets/qemu/qemu-wasm-rev.txt` exists from this plan onward, containing either a real short SHA or the literal string `unknown` so the binary always has *something* to embed."
    - "`make qemu-assets` writes a freshly-computed `git -C qemu-wasm rev-parse --short HEAD` into the file on every rebuild."
    - "`make clean-qemu-assets` removes the file alongside the other generated artifacts."
    - "The `include_dir!`-captured embed::QEMU::get_file(\"qemu-wasm-rev.txt\") lookup compiles and resolves to the committed file."
  artifacts:
    - path: "Makefile"
      provides: "Extended `qemu-assets` recipe writes qemu-wasm-rev.txt; extended `clean-qemu-assets` removes it."
      contains: "qemu-wasm-rev.txt"
    - path: "crates/bootroom/assets/qemu/qemu-wasm-rev.txt"
      provides: "Sentinel embedded file (one line, trailing newline OK). Initially `unknown`; will be overwritten by next `make qemu-assets`."
      contains: ""
  key_links:
    - from: "Makefile qemu-assets target"
      to: "crates/bootroom/assets/qemu/qemu-wasm-rev.txt"
      via: "git -C qemu-wasm rev-parse --short HEAD >"
      pattern: "qemu-wasm-rev.txt"
    - from: "crates/bootroom/src/embed.rs"
      to: "qemu-wasm-rev.txt"
      via: "include_dir! captures the file at compile time"
      pattern: "include_dir"
---

<objective>
Extend the `make qemu-assets` Makefile recipe to write the qemu-wasm submodule's short SHA into `crates/bootroom/assets/qemu/qemu-wasm-rev.txt`, then commit a sentinel value (`unknown\n`) for that file NOW so the next `cargo build -p bootroom` already picks it up via `include_dir!`. The doctor's `qemu_wasm_rev` check (lands in 05-04) reads this file via `embed::QEMU.get_file("qemu-wasm-rev.txt").and_then(|f| f.contents_utf8()).unwrap_or("unknown")` — but per Research Pitfall 2, the on-disk file is the more robust path because it survives across branches and avoids requiring an immediate full `make qemu-assets` rerun.

Purpose: Make `bootroom doctor` able to display the qemu-wasm rev verbatim per CONTEXT.md D-DOC-05 without forcing every developer to re-run the 10–30-minute docker build.

Output: Updated `Makefile` (one new step + one `clean` line); committed sentinel `qemu-wasm-rev.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-diagnostics-doctor/05-CONTEXT.md
@.planning/phases/05-diagnostics-doctor/05-RESEARCH.md
@Makefile
@crates/bootroom/build.rs

<interfaces>
<!-- Existing Makefile qemu-assets target structure (from grep): -->
<!-- - .PHONY at line 10 -->
<!-- - QEMU_WASM_DIR := qemu-wasm at line 12 -->
<!-- - QEMU_BUILDER := build-qemu-wasm at line 14 -->
<!-- - help text mentions "Step 1/5" through current final step -->
<!-- - clean-qemu-assets at line 71 -->

<!-- Required addition (Research §"Makefile qemu-assets extension"): -->
<!--   Add new Step 5b BEFORE the final echo lines: -->

```make
@echo ">>> Step 5b/5: Recording qemu-wasm git rev..."
@git -C $(QEMU_WASM_DIR) rev-parse --short HEAD > $(QEMU_OUT_DIR)/qemu-wasm-rev.txt
@echo "  qemu-wasm rev: $$(cat $(QEMU_OUT_DIR)/qemu-wasm-rev.txt)"
```

<!-- Update clean-qemu-assets to also remove the rev file. -->
<!-- The exact target name is QEMU_OUT_DIR; confirm it resolves to crates/bootroom/assets/qemu. -->

<!-- Sentinel file format: single line "unknown" with a trailing newline. -->
<!-- doctor_cmd.rs (05-04) trims whitespace before display. -->

<!-- build.rs MUST NOT add qemu-wasm-rev.txt to its REQUIRED list -->
<!-- (Research §"Makefile qemu-assets extension" + Pitfall 3). -->
<!-- This plan does NOT touch build.rs — explicit non-modification. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit sentinel `qemu-wasm-rev.txt` so include_dir! has something to embed today</name>
  <files>crates/bootroom/assets/qemu/qemu-wasm-rev.txt</files>
  <action>Create `crates/bootroom/assets/qemu/qemu-wasm-rev.txt` containing exactly the literal text `unknown` followed by a single trailing newline. No quotes, no leading whitespace, no UTF-8 BOM.

Rationale (per Pitfall 2 + Pattern 5):
- The next `cargo build -p bootroom` should pick up this file via the existing `include_dir!` capture in `crates/bootroom/src/embed.rs` (no embed.rs change is needed — `include_dir!` ingests every regular file under `assets/qemu/` automatically).
- A real SHA value will be written here the next time `make qemu-assets` runs. Until then, `unknown` is the documented degraded display string per D-DOC-05.
- Do NOT skip this step expecting `make qemu-assets` to be re-run in this plan. The docker build is a 10–30-minute operation gated on disk space (STATE.md notes 98% disk usage at last attempt); this plan must not block on it.

Verify the file's bytes match the spec exactly (`unknown\n`, 8 bytes total). Use the Write tool, not `echo`.

Do NOT modify `build.rs`. Specifically, do NOT add `qemu-wasm-rev.txt` to the REQUIRED list in build.rs — per Pitfall 3, the file's absence must remain non-fatal even on branches that lack it.</action>
  <verify>
    <automated>test -f crates/bootroom/assets/qemu/qemu-wasm-rev.txt &amp;&amp; test "$(wc -c &lt; crates/bootroom/assets/qemu/qemu-wasm-rev.txt)" = "8" &amp;&amp; grep -v '^#' crates/bootroom/build.rs | grep -c 'qemu-wasm-rev.txt' | grep -q '^0$'</automated>
  </verify>
  <done>File exists, is exactly 8 bytes (`unknown\n`), and is NOT referenced in `build.rs` REQUIRED list. `cargo build -p bootroom` still succeeds (the file is captured by `include_dir!` automatically; no embed.rs edits required).</done>
</task>

<task type="auto">
  <name>Task 2: Extend Makefile `qemu-assets` recipe + update `clean-qemu-assets`</name>
  <files>Makefile</files>
  <action>Locate the `qemu-assets` recipe in `Makefile` (begins at line 24 per the discovery scan). Append a new step IMMEDIATELY before the recipe's existing final echo block — pattern is "Step 5b/5" since the help text already advertises 5 steps but we are adding a sub-step in the same logical phase (alternative phrasing is acceptable; the key constraint is the step lands AFTER asset copies and BEFORE the final user-facing summary line so the output ordering makes sense).

Required behavior:
1. Step writes `git -C $(QEMU_WASM_DIR) rev-parse --short HEAD` to `$(QEMU_OUT_DIR)/qemu-wasm-rev.txt`. Confirm `QEMU_OUT_DIR` resolves to `crates/bootroom/assets/qemu` before relying on it; if the existing Makefile uses a different variable name, use whichever name the existing artifact copies use (must match line of sight with the existing wasm/data/worker.js outputs — they all land in the same directory).
2. Step echoes the recorded rev for operator confirmation: `@echo "  qemu-wasm rev: $$(cat $(QEMU_OUT_DIR)/qemu-wasm-rev.txt)"`.
3. Recipe MUST tolerate the qemu-wasm submodule being in any branch / detached HEAD state — `git rev-parse --short HEAD` works in all of those.
4. Update `clean-qemu-assets` (line 71) to also `rm -f $(QEMU_OUT_DIR)/qemu-wasm-rev.txt`. Do NOT use `rm -rf` (per AGENTS.md system safety).
5. Help text update is OPTIONAL but encouraged — if the existing help string is "Step 1/5...Step 5/5", adjust to "...Step 5b/5" or "5 of 6" so the count is honest.

Do NOT add a CI dependency on running `make qemu-assets`. The committed sentinel from Task 1 is sufficient for everyday development.

Test the recipe shape (NOT a full run — docker build is out of scope for this plan):
- `grep -c 'qemu-wasm-rev.txt' Makefile` returns at least 2 (write + clean).
- `make -n qemu-assets 2>&1 | grep -q 'rev-parse'` proves the recipe parses and the new step is wired into the chain (dry-run).</action>
  <verify>
    <automated>grep -v '^#' Makefile | grep -c 'qemu-wasm-rev.txt' | awk '$1 &gt;= 2'</automated>
  </verify>
  <done>`Makefile` references `qemu-wasm-rev.txt` in at least two places (one in the write step within `qemu-assets`, one in `clean-qemu-assets`). `make -n qemu-assets` parses successfully and the dry-run includes `rev-parse`. `make help` (if invoked) still renders cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| qemu-wasm submodule git state → embedded text file | The recorded SHA reflects whatever commit the submodule is currently checked out to. Crosses from "submodule git state" into "shipped binary embedded asset". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-02-01 | Tampering | Sentinel `unknown` could mask an actual mismatch | accept | Doctor reports the value verbatim; the operator interprets `unknown` as "rebuild qemu-wasm to refresh". Same posture as the build.rs `unknown` SHA. |
| T-05-02-02 | Information Disclosure | Embedded SHA in shipped binary | accept | Submodule SHA is public on the source side; no secret material. |
| T-05-02-03 | Tampering | Build-host substitutes a different qemu-wasm submodule | accept | Trust boundary is "build host produces the binary" — out of scope here (same posture as any embedded asset). |
| T-05-02-04 | Denial of Service | `make qemu-assets` fails partway through and leaves a stale rev | mitigate | Recipe order: rev write happens AFTER asset copies. If a copy fails, the recipe aborts before the rev write, and the previous rev (or sentinel) remains on disk consistent with the still-present previous artifacts. |
</threat_model>

<verification>
- File `crates/bootroom/assets/qemu/qemu-wasm-rev.txt` exists in `git status` as a tracked file (commit it in the plan's git step).
- `cargo build -p bootroom` succeeds without regressions.
- `cargo test --package bootroom` continues to pass (no test depends on the file's contents yet — 05-04 / 05-05 add tests against the embed lookup).
- Makefile recipe dry-runs cleanly: `make -n qemu-assets` produces no parse errors and includes the `rev-parse` invocation.
</verification>

<success_criteria>
- [ ] `crates/bootroom/assets/qemu/qemu-wasm-rev.txt` exists, is 8 bytes (`unknown\n`).
- [ ] `Makefile` references the file in two places (write + clean).
- [ ] `make -n qemu-assets` shows the new `rev-parse` step.
- [ ] `build.rs` REQUIRED list is unchanged (Pitfall 3 — file absence must remain non-fatal).
- [ ] No new dependencies introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-diagnostics-doctor/05-02-SUMMARY.md` listing:
- The exact line numbers / step ordering chosen inside the Makefile recipe.
- Confirmation that `cargo build -p bootroom` still succeeds.
- The resolved value of `QEMU_OUT_DIR` as written in the Makefile (so 05-04 references the right path if needed).
</output>
