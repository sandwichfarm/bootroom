---
phase: 02-websocket-live-serial
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bootroom/web/index.html
  - crates/bootroom/web/style.css
autonomous: true
requirements: [UI-04, UI-06, UI-08, UI-09]
must_haves:
  truths:
    - "Header strip contains four interactive elements in order: wordmark, kernel-info dl, [LAUNCH][RESET] button group, status pill (with the pill keeping `margin-left: auto`)"
    - "Terminal container has [CLEAR][COPY] controls overlaid in its top-right corner (position: absolute, z-index above xterm renderer)"
    - "Status pill supports a new `data-state=\"IDLE\"` value rendering with `var(--fg-muted)` background"
    - "All four new buttons use the existing palette tokens only — ZERO new hex values in style.css"
    - "All buttons are real `<button>` elements with visible text labels (no icon fonts, no aria-label hacks)"
    - "Initial pill state in HTML is `data-state=\"IDLE\"` (Phase 1's hardcoded `LOADING` is replaced)"
  artifacts:
    - path: "crates/bootroom/web/index.html"
      provides: "LAUNCH + RESET buttons in header; CLEAR + COPY controls inside #terminal; initial pill state IDLE"
      contains: "btn-launch"
    - path: "crates/bootroom/web/style.css"
      provides: ".hdr-btns + .term-ctrls + .pill[data-state=\"IDLE\"] CSS hooks reusing palette tokens"
      contains: "hdr-btns"
  key_links:
    - from: "crates/bootroom/web/index.html (#hdr)"
      to: "crates/bootroom/web/style.css (.hdr-btns)"
      via: "class selector"
      pattern: "\\.hdr-btns"
    - from: "crates/bootroom/web/index.html (#terminal > .term-ctrls)"
      to: "crates/bootroom/web/style.css (.term-ctrls)"
      via: "position: absolute inside the existing position: relative on #terminal"
      pattern: "position:\\s*absolute"
---

<objective>
Land the static UI surface deltas required by Phase 2 per 02-UI-SPEC.md: the LAUNCH/RESET button group in the header, the CLEAR/COPY controls overlaid on the terminal frame, and the new IDLE pill state. No JS behavior in this plan — the buttons are dead until plan 06 wires their click handlers. This separation keeps the visual-only change reviewable in isolation and lets plan 06 focus on the WS lifecycle / state machine work.

Purpose: Surface affordances for UI-04 (clear/copy controls), UI-06 (Idle pill state), UI-08 (Launch button), UI-09 (Reset button). Behavior is plan 06.
Output: Updated HTML markup, updated CSS with four new selector groups from 02-UI-SPEC.md "CSS hooks the executor must add to style.css".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-websocket-live-serial/02-CONTEXT.md
@.planning/phases/02-websocket-live-serial/02-UI-SPEC.md
@.planning/phases/01-walking-skeleton/01-UI-SPEC.md
@crates/bootroom/web/index.html
@crates/bootroom/web/style.css
@crates/bootroom/web/app.js

<interfaces>
DOM IDs that plan 06 will query (these are the contract this plan exposes to plan 06):
- `btn-launch` (header button)
- `btn-reset` (header button)
- `btn-clear` (terminal-overlay button)
- `btn-copy` (terminal-overlay button)
- `status` (existing — pill; this plan changes its initial `data-state` from `LOADING` to `IDLE`)

CSS selectors plan 06 may rely on:
- `.hdr-btns button:disabled` styling for the WS-reconnect-in-progress disabled state
- `.pill[data-state="IDLE"]` for IDLE

Palette constraint (02-UI-SPEC.md `## Color`): ZERO new hex values may be added to `:root` or anywhere else in style.css. All new selectors paint from existing CSS custom properties only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Launch/Reset/Clear/Copy markup + initial IDLE pill state to index.html</name>
  <files>crates/bootroom/web/index.html</files>
  <action>
    Edit `crates/bootroom/web/index.html`. Three surgical changes:

    1. Inside `<header id="hdr">`, BETWEEN `<dl class="kinfo">…</dl>` and `<span id="status" class="pill" …>`, insert a div with two buttons: a `<div class="hdr-btns">` containing `<button id="btn-launch" type="button">LAUNCH</button>` and `<button id="btn-reset" type="button">RESET</button>`. Per 02-UI-SPEC.md "Layout summary" (lines 167–181) and "Component Inventory" (line 160). Both buttons MUST have `type="button"` to prevent any future enclosing form from making them submit-buttons. Plain text labels — no icons, no nested span, no aria-label (the visible text IS the accessible name per 02-UI-SPEC accessibility floor).

    2. The existing `<div id="terminal"></div>` is self-closing. Convert it to an opening/closing pair and nest a `<div class="term-ctrls">` as its first child containing `<button id="btn-clear" type="button">CLEAR</button>` and `<button id="btn-copy" type="button">COPY</button>`. Per 02-UI-SPEC.md "Component Inventory" entry "Terminal controls overlay" (line 161). The xterm.js mount call in app.js (line 130) appends xterm's render layers to the container; the .term-ctrls placed BEFORE the mount will be a sibling that xterm renders BELOW in DOM order. Because .term-ctrls is `position: absolute` with `z-index: 10` (CSS in task 2), it floats over xterm's render. Verified pattern.

    3. In the existing `<span id="status" class="pill" data-state="LOADING">`, change `data-state="LOADING"` to `data-state="IDLE"`, and change the inner text from `LOADING` to `IDLE` (keeping the existing `<span aria-hidden="true">●</span>` glyph prefix). This is the per-02-UI-SPEC initial state — JS in plan 06 transitions to LOADING after xterm mount and to RUNNING after `Module.onRuntimeInitialized` plus first SerialOut byte.

    Do NOT modify the inline SAB-probe `<script>` block, the iso-banner div, the vendor `<script>` includes, or the `<script type="module" src="/assets/web/app.js">` reference. Plan 06 modifies app.js; this plan does not.

    Per CLAUDE.md project constraint: no emojis. Wayland system; UI tested in Chromium per AGENTS.md.
  </action>
  <verify>
    <automated>grep -q 'id="btn-launch"' crates/bootroom/web/index.html &amp;&amp; grep -q 'id="btn-reset"' crates/bootroom/web/index.html &amp;&amp; grep -q 'id="btn-clear"' crates/bootroom/web/index.html &amp;&amp; grep -q 'id="btn-copy"' crates/bootroom/web/index.html &amp;&amp; grep -q 'data-state="IDLE"' crates/bootroom/web/index.html &amp;&amp; ! grep -q 'data-state="LOADING"' crates/bootroom/web/index.html &amp;&amp; grep -q 'class="hdr-btns"' crates/bootroom/web/index.html &amp;&amp; grep -q 'class="term-ctrls"' crates/bootroom/web/index.html</automated>
  </verify>
  <done>
    All four button IDs present in HTML. Header has `.hdr-btns` container; terminal has `.term-ctrls` container. Initial pill state is IDLE (the previous LOADING literal is gone). HTML is well-formed (manual smoke: `cargo run -p bootroom -- serve --kernel /tmp/anyfile --no-open` then load the page — no parse errors in DevTools console).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS hooks for buttons and IDLE pill — palette-tokens-only, no new hex</name>
  <files>crates/bootroom/web/style.css</files>
  <action>
    Edit `crates/bootroom/web/style.css`. APPEND the following selector groups at the end of the file (after the existing `#terminal` and `.xterm*` rules). Source: 02-UI-SPEC.md lines 187–222.

    Use a comment-block header like `/* Phase 2 buttons + IDLE pill (UI-SPEC additions) */`.

    Then add these rules:

    - `.hdr-btns` selector: `display: flex; gap: var(--sm);`
    - Shared mono-button styling on `.hdr-btns button, .term-ctrls button` selector: `background: var(--surface); color: var(--fg); border: 0; padding: var(--sm) var(--md); font: inherit; font-size: 12px; font-weight: 600; line-height: 1.4; text-transform: uppercase; letter-spacing: 0.05em; cursor: pointer;`
    - `:hover` selector group: `box-shadow: inset 0 1px 0 0 var(--fg-muted);`
    - `:focus-visible` selector group: `outline: 1px solid var(--accent); outline-offset: 1px;`
    - `:disabled` selector group: `color: var(--fg-muted); cursor: not-allowed;`
    - `.term-ctrls` selector: `position: absolute; top: var(--sm); right: var(--sm); z-index: 10; display: flex; gap: var(--sm);`
    - `.pill[data-state="IDLE"]` selector: `background: var(--fg-muted); color: var(--fg);`

    Per 02-UI-SPEC.md "Idle pill color decision" (lines 91–98), the IDLE pill text color is `var(--fg)` on `var(--fg-muted)` background. The UI-SPEC notes that the executor must verify WCAG AA 4.5:1 contrast — `#C0CAF5` on `#7A8295`; if Phase 2 manual smoke shows the IDLE pill is unreadable, change to `var(--bg)` in a follow-up.

    Per 02-UI-SPEC.md "Color" line 80: accent is RESERVED for wordmark, sha256 prefix, and `:focus-visible` ring ONLY. The button hover effect MUST be `box-shadow: inset 0 1px 0 0 var(--fg-muted)` (NOT an accent border) — this is the "raised effect with palette colors only" requirement. Resting state uses no border/no shadow (terminal aesthetic is flat).

    No transitions on hover (terminal feel is immediate, not animated) per 02-UI-SPEC.md "Button hover state" (lines 110–119).

    Strict rule (02-UI-SPEC.md `## Diff Summary`): ZERO new hex values may appear in style.css. Verify with the grep gate in the verify block below — it counts existing 6-digit hex values across the file and asserts the count stays at 11 (the Phase 1 palette).

    Per CLAUDE.md: no emojis in code.
  </action>
  <verify>
    <automated>HEX_COUNT=$(grep -o "#[0-9a-fA-F]\{6\}" crates/bootroom/web/style.css | wc -l) &amp;&amp; test "$HEX_COUNT" -eq 11 &amp;&amp; grep -q "\.hdr-btns" crates/bootroom/web/style.css &amp;&amp; grep -q "\.term-ctrls" crates/bootroom/web/style.css &amp;&amp; grep -q 'data-state="IDLE"' crates/bootroom/web/style.css &amp;&amp; grep -q "focus-visible" crates/bootroom/web/style.css &amp;&amp; grep -q "inset 0 1px 0 0 var(--fg-muted)" crates/bootroom/web/style.css</automated>
  </verify>
  <done>
    `grep -o '#[0-9a-fA-F]\{6\}' style.css | wc -l` equals 11 (no new hex values introduced — the Phase 1 palette in `:root` is unchanged). `.hdr-btns`, `.term-ctrls`, `.pill[data-state="IDLE"]`, `:focus-visible`, and the `inset 0 1px 0 0 var(--fg-muted)` hover hook are all present. Manual smoke: page renders with the new buttons styled per 02-UI-SPEC; focus ring visible only on keyboard navigation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static HTML and CSS additions. No new origins, no new input surfaces, no new fetches. Plan 06 will wire button click handlers — that plan owns the JS-side threat model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | A future DOM-injection vulnerability (none in Phase 1) lets an attacker click LAUNCH/RESET and force a page reload | accept | Plan 06's reload-on-click is identical to the user pressing F5; no privilege escalation. Phase 1 has no XSS surface (no user-input rendering); Phase 2 introduces none. |
| T-02-12 | Information Disclosure | CSS does not affect the threat surface | accept | Visual styling only; no data exposure. |
| T-02-13 | UI Spoofing | Buttons could be styled to mimic system UI | accept | UI-SPEC's terminal aesthetic IS the styling — flat, monospace, terse. The buttons read as terminal commands, not system dialogs. No spoofing surface. |
</threat_model>

<verification>
- HTML parses cleanly in Chromium devtools (no parse errors after loading `/`).
- The four button DOM IDs are present and queryable via `document.getElementById`.
- CSS does not introduce any new hex literals (palette count remains 11).
- `.hdr-btns` and `.term-ctrls` use only the existing CSS custom properties.
- Buttons are visually distinguishable as interactive (cursor: pointer; visible focus ring on tab).
- The initial pill state is IDLE before any JS runs.
</verification>

<success_criteria>
- UI-04, UI-06, UI-08, UI-09 surface affordances exist and are styled per 02-UI-SPEC.
- ZERO new hex values introduced.
- Buttons are real `<button>` elements with text labels (a11y compliant per UI-SPEC accessibility floor).
- Plan 06 has a stable DOM contract to query against: btn-launch, btn-reset, btn-clear, btn-copy, status.
</success_criteria>

<output>
After completion, create `.planning/phases/02-websocket-live-serial/02-05-SUMMARY.md` per the standard summary template.
</output>
