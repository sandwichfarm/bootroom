---
phase: 06-distribution
plan: 07
type: execute
wave: 4
depends_on:
  - 06-04
  - 06-06
files_modified:
  - README.md
autonomous: true
requirements:
  - DIST-02
  - DIST-03
  - DIST-04
  - DIST-06
must_haves:
  truths:
    - "README's top-of-file region (above the install matrix) displays status / license / crates.io badges."
    - "A `## Install` section presents the three install paths in primary-secondary-tertiary order: `cargo install --locked bootroom`, `cargo binstall bootroom`, and the `curl ... bootroom-installer.sh | sh` one-liner."
    - "A `## Quickstart` section walks the user through `bootroom doctor` and then `bootroom serve --kernel <path>` — the canonical first-run flow."
    - "The README links to NORN as the first downstream consumer."
    - "README does NOT contain stale references to `cargo build --release` as the install path (that pre-Phase-6 quickstart is replaced by the install matrix)."
  artifacts:
    - path: "README.md"
      provides: "Top-of-file install matrix + quickstart + first-consumer reference, replacing the existing pre-Phase-6 quickstart block."
      contains: "## Install"
  key_links:
    - from: "README install matrix"
      to: "Released artifacts (cargo-dist generated)"
      via: "`curl https://github.com/sandwich-farm/bootroom/releases/latest/download/bootroom-installer.sh | sh`"
      pattern: "bootroom-installer.sh"
    - from: "README quickstart"
      to: "Phase-5 bootroom doctor + Phase-1 bootroom serve"
      via: "Documented command lines"
      pattern: "bootroom doctor.*bootroom serve"
---

<objective>
Update README.md with a top-of-file install matrix (the three locked install paths from CONTEXT.md D-07) and a tight quickstart (`bootroom doctor` then `bootroom serve --kernel <path>`, per CONTEXT.md `<decisions>`). Add status / crates.io / license badges row. Link NORN as the first consumer. Replace the existing pre-Phase-6 placeholder quickstart that suggests `cargo build --release` (which is correct for source builds but not the canonical install path post-Phase-6).

Purpose: CONTEXT.md D-07 mandates the install matrix is the README's top-of-file content. The quickstart is the user's first-run experience after installing. Keeping both terse (CONTEXT.md `<decisions>`: "Keep terse — point to repo docs for deeper reads") avoids the README becoming a docs dumping ground. This plan is the README polish layer; install paths landed in 06-03/06-04/06-06; this plan just documents them.

Output: A single updated `README.md`. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-distribution/06-CONTEXT.md
@.planning/phases/06-distribution/06-01-SUMMARY.md
@.planning/phases/06-distribution/06-04-SUMMARY.md
@.planning/phases/06-distribution/06-06-SUMMARY.md
@README.md

<interfaces>
The README currently contains (post-06-01 Task 3):

```markdown
# bootroom

A web-based test harness for RISC-V kernels (and any qemu-wasm guest).
Press one button, get the freshest kernel running in a browser with a
click-to-trigger scenario library.

[![License: MIT OR Apache-2.0](https://img.shields.io/badge/license-MIT%20OR%20Apache--2.0-blue.svg)](#license)

> Note: the working directory on this development host is still
> `~/Develop/norn-web` from the project's earlier name; the workspace,
> binary, and crates are all named `bootroom`. The directory may be
> renamed at the user's convenience.

## Quickstart

    cargo build --release
    ./target/release/bootroom serve --kernel /path/to/Image
    # Open http://127.0.0.1:8765

## License

Dual-licensed under MIT OR Apache-2.0. See LICENSE-MIT and LICENSE-APACHE.
```

The install matrix (CONTEXT.md D-07):
- Primary: `cargo install --locked bootroom`
- Secondary: `cargo binstall bootroom`
- Tertiary: `curl https://github.com/sandwich-farm/bootroom/releases/latest/download/bootroom-installer.sh | sh`

The quickstart (CONTEXT.md `<decisions>`):
- `bootroom doctor`
- `bootroom serve --kernel <path>`

Status badges (cargo-dist + crates.io conventions):
- Build status: `https://github.com/sandwich-farm/bootroom/actions/workflows/release.yml/badge.svg`
- crates.io version: `https://img.shields.io/crates/v/bootroom.svg`
- docs.rs: `https://docs.rs/bootroom/badge.svg`
- License: existing badge from 06-01 Task 3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md top-of-file section with install matrix + quickstart</name>
  <files>README.md</files>
  <action>Replace the existing `## Quickstart` section with a full top-of-file rework. The bottom-of-file `## License` section stays intact. The H1 + tagline + repo-rename note + license badge from 06-01 also stay intact.

Final README shape (after this task):

```markdown
# bootroom

A web-based test harness for RISC-V kernels (and any qemu-wasm guest).
Press one button, get the freshest kernel running in a browser with a
click-to-trigger scenario library.

[![License: MIT OR Apache-2.0](https://img.shields.io/badge/license-MIT%20OR%20Apache--2.0-blue.svg)](#license)
[![crates.io](https://img.shields.io/crates/v/bootroom.svg)](https://crates.io/crates/bootroom)
[![docs.rs](https://docs.rs/bootroom/badge.svg)](https://docs.rs/bootroom)
[![release](https://github.com/sandwich-farm/bootroom/actions/workflows/release.yml/badge.svg)](https://github.com/sandwich-farm/bootroom/actions/workflows/release.yml)

> Note: the working directory on this development host is still
> `~/Develop/norn-web` from the project's earlier name; the workspace,
> binary, and crates are all named `bootroom`. The directory may be
> renamed at the user's convenience.

First consumer: the [NORN RISC-V kernel](https://github.com/sandwich-farm/nostros).
NORN's CI and local debug flows pull `bootroom` as an external dependency.

## Install

bootroom ships three install paths. Pick the one that matches your
environment.

| Path | Command | When to use |
|---|---|---|
| `cargo install` | `cargo install --locked bootroom` | Primary path for Rust-equipped developers. |
| `cargo binstall` | `cargo binstall bootroom` | Secondary path when you already have `cargo-binstall`; fetches a prebuilt release binary. |
| Shell installer | `curl --proto =https --tlsv1.2 -sSf https://github.com/sandwich-farm/bootroom/releases/latest/download/bootroom-installer.sh \| sh` | Tertiary path for kernel CI runners with no Rust toolchain. |

Supported platforms: `x86_64-unknown-linux-musl`, `aarch64-unknown-linux-musl`,
`x86_64-apple-darwin`, `aarch64-apple-darwin`. Windows and Linux GNU
(non-musl) targets are not currently provided.

## Quickstart

After install, run the preflight check:

    bootroom doctor

If `doctor` reports `overall: pass`, point bootroom at a kernel ELF:

    bootroom serve --kernel /path/to/Image

bootroom opens your default browser to the harness URL. Press the
on-screen Launch button to boot the kernel; use the action-button panel
(populated from `bootroom.toml`) to drive scenarios against the running
guest. For headless CI, swap `serve` for `run --scenario <name>` once
you have a config file.

For more, see `bootroom --help` or the `.planning/` directory in this
repo.

## Source builds and contribution

If you are working on bootroom itself rather than consuming it:

    git clone https://github.com/sandwich-farm/bootroom
    cd bootroom
    make qemu-assets   # one-time; rebuilds the embedded qemu-wasm artifacts (requires docker; 10-30 min)
    make install       # runs `cargo install --locked --path crates/bootroom`

`make help` lists the full Makefile target set.

## License

Dual-licensed under MIT OR Apache-2.0. See LICENSE-MIT and LICENSE-APACHE.
```

Implementation notes:

1. The H1 (`# bootroom`), the tagline paragraph, the repo-rename note, and the existing license badge from 06-01 Task 3 all remain at the top. Only the body below them changes.

2. The badges row adds three new badges (crates.io, docs.rs, release-workflow) below the existing license badge. Order: License → crates.io → docs.rs → release. Each on its own line — markdown renders them as a horizontal row.

3. The new "First consumer" line links NORN. The URL `https://github.com/sandwich-farm/nostros` matches PROJECT.md's `~/Develop/nostros` reference. If NORN's actual GitHub URL differs, fix it at the user's call.

4. The `## Install` section uses a 3-row markdown table. Note the backslash-escaped pipe in the shell-installer command (`\|`) — markdown tables require literal `|` to be escaped or wrapped in backticks. The whole command is inside backticks already, which suffices in GitHub-flavored markdown. If GitHub's renderer eats the pipe, swap to a code block under each row.

5. The `curl ... | sh` command includes `--proto =https --tlsv1.2 -sSf` — the cargo-dist-recommended hardened curl flags. These are NOT optional; the un-hardened `curl | sh` pattern is the canonical insecure version. The recommended flags match cargo-dist's docs at https://opensource.axo.dev/cargo-dist/book/installers/shell.html.

6. The `## Quickstart` section is two commands plus a single explanatory paragraph. Do not expand it.

7. The `## Source builds and contribution` section absorbs the existing `cargo build --release` quickstart for the rare case the user is hacking on bootroom itself rather than consuming it. Three commands; minimal prose. Mentions `make help` for discoverability of the full Makefile surface (which 06-04 expanded with `install` and `release` targets).

8. Do NOT add a `## Configuration` section, a TOML example block, an architecture diagram, or a feature matrix. CONTEXT.md `<decisions>` is explicit: "Keep terse." Point at the `.planning/` directory for anyone who wants depth.

9. Do NOT remove the existing repo-rename note. It is informative for anyone clicking through during the transition window.

10. The shell-installer URL uses the future canonical `https://github.com/sandwich-farm/bootroom` per the rename caveat in 06-PLAN.md. If the GitHub remote is still under a different org/name at release time, a single search-and-replace edits the URL — but the URL value is the same as `[workspace.package].repository` so it tracks consistently.

11. The badge URLs use the future canonical bootroom name (`crates.io/crates/bootroom`, `docs.rs/bootroom`). Until the first publish lands these will 404; that is acceptable (and helpfully visible) during the transition.</action>
  <verify>
    <automated>grep -Fq "## Install" README.md && grep -Fq "## Quickstart" README.md && grep -Fq "cargo install --locked bootroom" README.md && grep -Fq "cargo binstall bootroom" README.md && grep -Fq "bootroom-installer.sh" README.md && grep -Fq "bootroom doctor" README.md && grep -Fq "bootroom serve --kernel" README.md && grep -Fq "--proto =https --tlsv1.2 -sSf" README.md && grep -Fq "First consumer" README.md && grep -Fq "nostros" README.md && grep -Fq "crates.io/crates/bootroom" README.md && grep -Fq "docs.rs/bootroom" README.md && grep -Eq '^## License$' README.md && ! grep -Eq '^[[:space:]]*cargo build --release[[:space:]]*$' README.md</automated>
  </verify>
  <done>README.md has top-of-file install matrix (3 paths), quickstart (doctor → serve), NORN consumer reference, badge row (license + crates.io + docs.rs + release-workflow), and the existing `## License` section preserved. The stale `cargo build --release` standalone quickstart is gone; it survives only as part of the lower-down `## Source builds and contribution` section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User's terminal → curl-installer pipeline | The `curl ... \| sh` install path executes downloaded shell over a user's shell. The hardened flags (`--proto =https --tlsv1.2 -sSf`) reduce attack surface (TLS, no plaintext fallback, fail-on-error). |
| README badges → external services | Badge images are loaded from img.shields.io, docs.rs, and the GitHub Actions badge service. Static GETs; no auth; trustable as informational. |
| Documented install commands → user's environment | Each documented command is straightforward shell. No env-var injection, no credential prompts. The shell-installer's URL is repo-pinned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-07-01 | Tampering | Documented `curl \| sh` command omits hardened flags | mitigate | Task 1 includes `--proto =https --tlsv1.2 -sSf` per cargo-dist's published recommendation. Verification gate checks the flag string. |
| T-06-07-02 | Spoofing | Shell-installer URL points at the wrong org/repo | mitigate | URL uses `[workspace.package].repository` value; matches the canonical project URL. Future rename is a one-shot search-and-replace. |
| T-06-07-03 | Repudiation | Documented commands don't pin a version | accept | `cargo install --locked bootroom` installs the latest published version. Pinning is `cargo install --locked bootroom@<version>`; documented as advanced usage outside this terse README. |
| T-06-07-04 | Information Disclosure | Badge image loads leak referer to badge services | accept | Standard GitHub-rendered-README behavior. No PII transmitted; only Referer header with the repo URL. |
| T-06-07-05 | Denial of Service | Badge service downtime makes README ugly | accept | Cosmetic only. Badges fall back to alt text. |
| T-06-07-06 | Tampering | Documentation drift between README and actual release behavior | mitigate | The README is the user-facing surface; mismatches show up immediately during the first release. The `release-smoke` workflow (06-06) and the post-release manual smoke catch drift. |
| T-06-07-07 | Elevation of Privilege | A NORN consumer trusts the README's quickstart and the binary's effective security model | accept | bootroom is a local dev tool bound to 127.0.0.1 by default. The README does not encourage exposing it externally. Authentication/multi-user is explicitly out of scope per REQUIREMENTS.md. |
</threat_model>

<verification>
- README.md has a `## Install` section.
- README.md has a `## Quickstart` section.
- README.md lists all three locked install paths (cargo install, cargo binstall, shell installer).
- The shell-installer command uses `--proto =https --tlsv1.2 -sSf`.
- README references NORN as first consumer.
- Badge row includes license, crates.io, docs.rs, and release-workflow badges.
- `## License` section preserved.
- The stale `cargo build --release` standalone quickstart is replaced (now absorbed into `## Source builds and contribution`).
- README contains no broken markdown (no unclosed code fences, no malformed tables).
</verification>

<success_criteria>
DIST-02/03/04/06 each have a documented user-facing surface. A new developer reading the README knows the three install paths, the quickstart, and where to find NORN as the canonical consumer. The README stays terse per CONTEXT.md decision; depth lives in `.planning/`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution/06-07-SUMMARY.md` with:
- The full final README.md contents (verbatim), OR a diff against the pre-task state.
- A list of any URLs left as placeholders (e.g. NORN's GitHub URL if uncertain) for the user to confirm.
</output>
