---
phase: 06-distribution
verified: 2026-05-19T22:15:00Z
status: human_needed
score: 3/4 must-haves verified (1 awaits live tag-push)
overrides_applied: 0
human_verification:
  - test: "Tag-push publishes a working `cargo install bootroom` to crates.io"
    expected: "After the first manual `cargo publish -p bootroom-core` + a `v0.1.0` tag push, `cargo install --locked bootroom` from a clean machine fetches and builds the published crate. `bootroom doctor` reports overall=pass."
    why_human: "DIST-03's crates.io publish step is NOT wired in any in-repo workflow (release.yml does not call `cargo publish`; release-smoke only simulates the install round-trip locally). The first publish of `bootroom-core` must be done by hand because `cargo package -p bootroom` verify fails until bootroom-core resolves from crates.io. 06-03 SUMMARY explicitly marks DIST-03 incomplete; README has a 'Pre-1.0 status' note acknowledging the gap."
  - test: "Tag-push triggers release.yml, builds 4 prebuilt binaries, and `gh release create` runs after release-smoke passes"
    expected: "Pushing a `v*.*.*` tag executes plan -> build-local-artifacts (4 targets) -> build-global-artifacts -> custom-release-smoke -> host (gh release create). A deliberately-failing smoke step in a test tag prevents `gh release create` from running. Prebuilt `bootroom-<target>.tar.xz` + `bootroom-installer.sh` appear on the GH Release."
    why_human: "release.yml fires only on `pull_request` and `push: tags: '**[0-9]+.[0-9]+.[0-9]+*'`. Cannot verify the full release pipeline (cargo-zigbuild cross-compile, gh release create, artifact upload) without an actual tag push to GitHub Actions. The gating edge (`host.needs: custom-release-smoke`) and the `if:` predicate are correctly wired in release.yml lines 225-233 — confirmed by static reading."
  - test: "`cargo binstall bootroom` auto-discovers prebuilt artifacts from the GH Release"
    expected: "After a successful tagged release, `cargo binstall bootroom` finds and installs the prebuilt artifact matching the host triple via the [package.repository] auto-discovery convention."
    why_human: "cargo-binstall reads `[package.repository]` (Cargo.toml:10 → github.com/sandwich-farm/bootroom) and matches artifact names against cargo-dist's `bootroom-<target>.tar.xz` convention. Cannot exercise without a real GH Release."
  - test: "DIST-05 path-independence test passes inside Docker rust:1.85-alpine after a real `make qemu-assets` populates qemu-wasm-rev.txt"
    expected: "The release-smoke 'DIST-05 path-independence check' step (release-smoke.yml:151-172) runs the install_smoke `path_independence_qemu_wasm_rev_present` test against a Docker-installed bootroom from /tmp. With a real qemu-wasm-rev.txt committed (not the `unknown` sentinel), the assertion `!rev_detail.contains('rev unknown')` succeeds."
    why_human: "Verified locally: running `target/release/bootroom doctor --format json` from /tmp reports `overall: pass` and exposes `qemu_wasm_rev` with status=info. However the committed `crates/bootroom/assets/qemu/qemu-wasm-rev.txt` currently contains the literal sentinel `unknown`, which would fail the test's degraded-sentinel assertion. This is the documented Phase 5 sentinel-until-make-qemu-assets posture; a tag push must follow a `make qemu-assets` + commit. Re-running the test under Docker with a real rev requires the tag-push environment."
---

# Phase 6: Distribution Verification Report

**Phase Goal:** A kernel project on any supported platform installs `bootroom` in one step — `cargo install --locked bootroom`, `cargo binstall bootroom`, or `curl ... bootroom-installer.sh | sh` — and runs from any working directory.

**Verified:** 2026-05-19T22:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria)

| #   | Truth                                                                                                              | Status                | Evidence                                                                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------ | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `make install` / `cargo install --path .` installs binary; runs from any CWD; dual MIT OR Apache-2.0 SPDX; cargo-deny enforces | VERIFIED              | Makefile L89-90 `install: cargo install --locked --path crates/bootroom`. `[workspace.package].license = "MIT OR Apache-2.0"` (Cargo.toml:9). LICENSE-MIT + LICENSE-APACHE present at repo root (WR-03: appendix copyright now `Copyright 2026 sandwich`). deny.toml `version = 2` in both `[licenses]` and `[advisories]`; ci-deny.yml runs `cargo deny check`. `cargo deny check licenses` and `cargo deny check advisories` both reported OK locally. Built binary executed from `/tmp` returns `overall: pass`. |
| 2   | Published to crates.io; `cargo install bootroom` from clean container produces working binary (smoke gates)        | PARTIAL (human_needed) | release-smoke is **wired correctly** as a pre-`host` gate (release.yml:225-233 — CR-04 hand-edit confirmed; `host.needs: custom-release-smoke` + `if:` predicate enforces smoke success). Release-smoke.yml runs `cargo install --locked --path` inside Docker `rust:1.85-alpine` and asserts `bootroom doctor` overall=pass. **However:** no `cargo publish` step exists in any workflow. 06-03 SUMMARY marks DIST-03 incomplete; README has a "Pre-1.0 status" caveat (README.md:25-29). Cannot fully verify until first manual `cargo publish -p bootroom-core` + tagged release. |
| 3   | Prebuilt release binaries via cargo-dist for 4 musl/darwin targets; `[package].include` lists web/ + qemu-wasm artifacts | VERIFIED              | dist-workspace.toml L13 pins the 4 locked targets (`x86_64-unknown-linux-musl`, `aarch64-unknown-linux-musl`, `x86_64-apple-darwin`, `aarch64-apple-darwin`). cargo-dist v0.31.0 pinned (dist-workspace.toml:7). release.yml generated with `plan -> build-local-artifacts (matrix) -> build-global-artifacts -> host -> announce` shape. `cargo package -p bootroom --no-verify --list` ships `web/` (10 files including vendor xterm.js/xterm-pty.js) + `assets/qemu/*.wasm/.data/.worker.js/load.js/out.js/qemu-wasm-rev.txt/module.js` + LICENSE-MIT + LICENSE-APACHE + README.md + src/**. `[package].include` allow-lists in both crates/bootroom/Cargo.toml:16-30 and crates/bootroom-core/Cargo.toml:16-21. |
| 4   | `cargo binstall bootroom` discovers and installs prebuilt artifacts automatically                                  | HUMAN_NEEDED          | `[workspace.package].repository = "https://github.com/sandwich-farm/bootroom"` (Cargo.toml:10) supplies the [package.repository] auto-discovery convention. cargo-dist emits `bootroom-<target>.tar.xz` artifact names matching binstall's default pattern (06-03 SUMMARY provides). No bespoke `[package.metadata.binstall]` needed — auto-discovery drops out for free. Cannot exercise without a real GH Release. |

**Score:** 3/4 truths verified; 1 partially verified (gated on tag-push CI execution which is not yet performed). Sub-checks for truths 2 + 3 + 4 require a live tag push to fully exercise — see human verification.

### Required Artifacts

| Artifact                                                | Expected                                                                                                            | Status     | Details                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Cargo.toml` (workspace)                                | `[workspace.package].license = "MIT OR Apache-2.0"`; `bootroom-core` dep has `version = "0.1.0"`                    | VERIFIED   | L9 license; L39 `bootroom-core = { path = "crates/bootroom-core", version = "0.1.0" }` (CR-01 fix landed). `[profile.dist]` inherits release with lto="thin" (L43-45). repository pinned to github.com/sandwich-farm/bootroom (L10).                                                                                                                                                                                       |
| `crates/bootroom/Cargo.toml`                            | Workspace inheritance for license/repo; `publish = true`; explicit `include` allow-list for src + web + qemu assets + license + README | VERIFIED | All workspace fields inherited (L3-8). publish=true L15. include allow-list L16-30 covers `src/**/*.rs`, `build.rs`, `web/**/*`, all 6 qemu assets, `/LICENSE-MIT`, `/LICENSE-APACHE`, `/README.md`. Description, homepage, keywords, categories all present.                                                                                                                                                              |
| `crates/bootroom-core/Cargo.toml`                       | Workspace inheritance; `publish = true`; include lists src + license + README                                       | VERIFIED   | L1-15 workspace inheritance + metadata fields. L16-21 include allow-list. `cargo package -p bootroom-core` succeeds (10 files, 21.6 KiB compressed).                                                                                                                                                                                                                                                                      |
| `LICENSE-MIT` + `LICENSE-APACHE`                        | Both files present at repo root; canonical text; copyright lines filled                                             | VERIFIED   | Both present. LICENSE-MIT has `Copyright (c) 2026 sandwich ...`. LICENSE-APACHE appendix now has `Copyright 2026 sandwich` (WR-03 fix line 189).                                                                                                                                                                                                                                                                          |
| `dist-workspace.toml`                                   | `[dist]` table pinning cargo-dist 0.31.0, 4 targets, shell+homebrew installers, host-jobs gate                       | VERIFIED   | cargo-dist-version="0.31.0" (L7); installers=["shell","homebrew"] (L11); targets=4 musl+darwin (L13); host-jobs=["./release-smoke"] (L32). Comment block L25-31 documents the CR-04 hand-edit caveat for `dist generate`.                                                                                                                                                                                                  |
| `deny.toml`                                             | License allow-list = MIT/Apache + permissive equivalents; `version = 2`; `yanked = "deny"`                          | VERIFIED   | `[licenses].version = 2` (L42); `[advisories].version = 2` (L118); `[advisories].yanked = "deny"` (L124). Allow-list includes MIT, Apache-2.0, Apache-2.0 WITH LLVM-exception, BSD-2/3, ISC, Unicode-*, Zlib, CC0-1.0, BSL-1.0, MPL-2.0 with rationale. `unknown-registry = "deny"`, `unknown-git = "deny"`.                                                                                                                |
| `Makefile` (install + release targets)                  | `make install` -> `cargo install --locked --path crates/bootroom`; `make release` -> `cargo dist build --artifacts=all` | VERIFIED   | L89-90 install target; L103-104 release target; help text L23-24 documents both.                                                                                                                                                                                                                                                                                                                                          |
| `.github/workflows/release.yml`                         | cargo-dist generated, `host.needs:` includes `custom-release-smoke`, `if:` predicate requires smoke success         | VERIFIED   | L226-230 `host.needs: [plan, build-local-artifacts, build-global-artifacts, custom-release-smoke]`. L233 `if:` predicate requires `needs.custom-release-smoke.result == 'skipped' || ... == 'success'`. CR-04 hand-edit confirmed in commit 40e643c. All `actions/upload-artifact` and `actions/download-artifact` references use `@v4` (WR-02 fix in commit 4d164e3).                                                       |
| `.github/workflows/release-smoke.yml`                   | Reusable workflow callable via `host-jobs`; Docker `cargo install` + `bootroom doctor` smoke; explicit DIST-05 step  | VERIFIED   | `on: workflow_call`. Three Docker steps with `${BOOTROOM_VERSION}` pinning (CR-02 fix in commit 14bc1bb): smoke + install_smoke tests + DIST-05 named step (L151-172). `cargo package` runs without `--allow-dirty --no-verify` (WR-05 fix). Operates from `/tmp` CWD inside container.                                                                                                                                    |
| `.github/workflows/ci-deny.yml`                         | Runs `cargo deny check` on push + PR to master                                                                      | VERIFIED   | `cargo install cargo-deny --locked --version ^0.19`; `cargo deny check --hide-inclusion-graph` on push + PR to master.                                                                                                                                                                                                                                                                                                    |
| `crates/bootroom/tests/install_smoke.rs`                | 3 `#[ignore]` tests; DIST-05 asserts `qemu_wasm_rev` status=`info` AND detail not `"rev unknown"`                    | VERIFIED   | `grep -c "#\\[ignore\\]"` returns 3. CR-03 assertion now expects `info` status (L122-126) matching documented `check_qemu_rev` contract; separate detail check asserts `!rev_detail.contains("rev unknown")` (L142-146). `cargo test --test install_smoke -p bootroom` compiles and lists 3 ignored tests.                                                                                                                |
| `README.md`                                             | Install matrix (cargo / binstall / curl); quickstart (doctor + serve); license badge                                | VERIFIED   | L7-10 dual-license + crates.io + docs.rs + release CI badges. L21-39 install matrix with three rows. L25-29 "Pre-1.0 status" caveat (WR-01 fix in commit 4d164e3) explaining the install matrix is dormant until first published release. L41-58 quickstart with `bootroom doctor` then `bootroom serve --kernel ...`.                                                                                                       |

### Key Link Verification

| From                              | To                                              | Via                                                   | Status      | Details                                                                                                                                                                                                                                                                                                 |
| --------------------------------- | ----------------------------------------------- | ----------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Workspace `Cargo.toml`            | `bootroom-core` crate at version 0.1.0          | `[workspace.dependencies].bootroom-core` w/ version    | WIRED       | `cargo package -p bootroom-core --no-verify` succeeds; manifest validation passes. `cargo package -p bootroom --no-verify` no longer errors on missing-version (manifest-validation green). Verify step legitimately fails on missing crates.io publish (documented WR-01 catch-22).                       |
| `release.yml host` job            | `custom-release-smoke` reusable workflow        | `needs:` edge + `if:` predicate                       | WIRED       | host.needs lists custom-release-smoke; if-predicate gates on success/skipped. The earlier defect (CR-04) where smoke ran in parallel with `gh release create` is closed.                                                                                                                                  |
| `release-smoke.yml`               | bootroom binary on PATH inside container        | tar extract -> `cargo install --locked --path`        | WIRED       | All three Docker steps use `${BOOTROOM_VERSION}` to extract bootroom-${VER}.crate and install from the literal path. No ambiguous glob (CR-02 fix). Binary invoked from `/tmp` exercising path-independence.                                                                                              |
| `install_smoke.rs` tests          | doctor's `check_qemu_rev` contract              | JSON status + detail assertions                       | WIRED       | Assertions match documented behaviour (`status == "info"`; detail-string degraded-sentinel check). CR-03 mis-assertion fixed.                                                                                                                                                                              |
| `[workspace.package].repository`  | cargo-binstall auto-discovery                   | `[package.repository] -> github.com/sandwich-farm/bootroom` + cargo-dist artifact naming | NOT_VERIFIED | Wiring is present in Cargo.toml; cannot verify without live GH Release artifacts. Listed under human verification.                                                                                                                                                                                       |
| Tag push (`v*.*.*`)               | crates.io publish                               | (none — no workflow step exists)                      | NOT_WIRED   | release.yml + release-smoke.yml contain no `cargo publish` invocation. 06-03 SUMMARY documents this as an accepted deviation. This is the WR-01 / DIST-03 gap.                                                                                                                                            |

### Data-Flow Trace (Level 4)

| Artifact                  | Data Variable                  | Source                                                          | Produces Real Data | Status              |
| ------------------------- | ------------------------------ | --------------------------------------------------------------- | ------------------ | ------------------- |
| `bootroom doctor` JSON    | `checks[].qemu_wasm_rev.detail` | `embed::QEMU.get_file("qemu-wasm-rev.txt")` (compile-time embed) | Yes (degraded)     | FLOWING (degraded)  |

Locally verified: `target/release/bootroom doctor --format json` run from `/tmp` returned `overall: pass`, `qemu_wasm_rev: {name: 'qemu_wasm_rev', status: 'info', detail: 'qemu-wasm rev unknown'}`. The data path is real (file embedded at compile time via include_dir!), but the committed file content is the documented Phase 5 sentinel `unknown` until `make qemu-assets` runs against the live qemu-wasm submodule. The path-independence test's degraded-sentinel assertion would fail today against the committed sentinel; this is operational, not structural — release-smoke is expected to run after a `make qemu-assets` + commit of the resulting rev.

### Behavioral Spot-Checks

| Behavior                                       | Command                                                                 | Result                                                                                                                                | Status |
| ---------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `cargo deny check licenses` succeeds            | `cargo deny check licenses`                                             | `licenses ok` (3 unencountered-allowance warnings for Unicode-DFS-2016, Zlib, BSL-1.0 — expected on the dep tree)                       | PASS   |
| `cargo deny check advisories` succeeds          | `cargo deny check advisories`                                           | `advisories ok`                                                                                                                       | PASS   |
| `cargo package -p bootroom-core --no-verify`    | as above                                                                | Packaged 10 files, 76.3KiB (21.6KiB compressed), no errors                                                                            | PASS   |
| `cargo package -p bootroom --no-verify --list`  | as above                                                                | Lists 30+ files including `web/` (10), `assets/qemu/*` (7), LICENSE-MIT, LICENSE-APACHE, README.md                                    | PASS   |
| `cargo build --release -p bootroom`             | as above                                                                | Compiles cleanly                                                                                                                      | PASS   |
| `bootroom doctor --format json` from `/tmp`     | `cd /tmp && /home/sandwich/.../target/release/bootroom doctor --format json` | `overall: pass`; `qemu_wasm_rev` check present with `status: info`                                                                  | PASS   |
| `install_smoke` tests compile and gate          | `cargo test --test install_smoke -p bootroom`                           | 3 ignored tests listed; build succeeds                                                                                               | PASS   |
| `cargo test -p bootroom --lib`                  | as above                                                                | 124 passed, 0 failed                                                                                                                  | PASS   |
| `cargo test -p bootroom-core`                   | as above                                                                | 0 tests (crate has no unit tests), exit 0                                                                                              | PASS   |
| `cargo package -p bootroom` verify (no flags)   | (skipped — would fail on missing crates.io bootroom-core)                | Documented in 06-REVIEW-FIX.md WR-05 notes; not run                                                                                  | SKIP   |

### Probe Execution

No phase-declared probes for Phase 6 (Phase 6 is documentation + CI wiring, not code with runnable probes). Skipped.

### Requirements Coverage

| Requirement | Source Plan(s)         | Description                                                                                                | Status                | Evidence                                                                                                                                                                       |
| ----------- | ---------------------- | ---------------------------------------------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| DIST-02     | 06-04 (also 06-07)     | Single command installs the binary locally (`make install` / `cargo install --path .`)                     | SATISFIED             | Makefile install target verified; ran `make install` shape locally via direct cargo invocation.                                                                                |
| DIST-03     | 06-03, 06-06, 06-07    | Published to crates.io; installable via `cargo install bootroom`                                           | BLOCKED (human_needed)| No `cargo publish` step in any workflow. 06-03 SUMMARY accepts this as a deviation; README has Pre-1.0 caveat. First publish must be manual.                                    |
| DIST-04     | 06-03 (also 06-07)     | Prebuilt release binaries for 4 musl + darwin targets via cargo-dist + GitHub Releases                     | SATISFIED (in-repo)   | dist-workspace.toml targets confirmed; release.yml builds the matrix on tag push. Live verification requires a tag push.                                                       |
| DIST-05     | 06-02, 06-08, 06-06    | Binary runs from any working directory; `include_dir!` embedding                                            | SATISFIED             | `cargo package --list` ships all assets; `bootroom doctor` from `/tmp` returns `overall: pass`; install_smoke `path_independence_qemu_wasm_rev_present` test exists and compiles. |
| DIST-06     | 06-03 (also 06-07)     | `cargo binstall bootroom` works automatically from release artifacts                                       | SATISFIED (in-repo)   | `[package.repository]` populated; cargo-dist artifact-naming convention matches binstall default. Live verification requires a real GH Release.                                |
| DIST-07     | 06-01, 06-05           | License is MIT OR Apache-2.0 (dual SPDX)                                                                   | SATISFIED             | Workspace license key set; LICENSE-MIT and LICENSE-APACHE present and properly filled. deny.toml enforces.                                                                       |

**Orphaned requirements:** None. All 6 DIST requirements appear in at least one plan's `requirements:` field.

### Anti-Patterns Found

| File                                              | Line(s)      | Pattern                                         | Severity | Impact                                                                                                                                                              |
| ------------------------------------------------- | ------------ | ----------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.github/workflows/release.yml`                   | 232-233      | Hand-edited cargo-dist-generated workflow       | Info     | The CR-04 + WR-02 fixes (gate edge + @v4 action pins) will be silently overwritten by any future `dist generate --mode ci` run. dist-workspace.toml has a comment (L25-31) warning maintainers; consider an upstream cargo-dist issue tracking `pre-host-jobs` / `gate-jobs` config.                                                       |
| `assets/qemu/qemu-wasm-rev.txt`                   | -            | Committed sentinel `unknown` (degraded value)   | Info     | This is Phase 5's intentional sentinel — `make qemu-assets` overwrites it with the real revision before tag push. Documented in Phase 5; not a Phase 6 regression. Release-smoke's degraded-sentinel assertion catches a regression where this file is shipped containing literal "unknown".  |
| `crates/bootroom/Cargo.toml`                      | various      | `[dev-dependencies]` references files not in include allow-list | Info     | `cargo package -p bootroom --no-verify` emitted warnings: `ignoring test ws_broadcast_fanout` (and two others) because `tests/*.rs` is not in the include allow-list. Intentional per the lean-publish posture (06-02 SUMMARY) — integration tests do not ship.    |

No BLOCKER or WARNING anti-patterns found.

### Debt Markers

None in Phase 6 modified files. No unreferenced TBD/FIXME/XXX markers detected.

### Human Verification Required

See the `human_verification:` block in the frontmatter. Summary:

1. **Tag-push publish of bootroom-core then bootroom to crates.io** — DIST-03 has no automated step. First publish must be manual (or a follow-up plan must wire a `cargo publish` step). Without this, `cargo install --locked bootroom` errors with "could not find 'bootroom' in registry 'crates-io'". The README's Pre-1.0 caveat documents the gap user-side.
2. **Tag-push full release.yml pipeline execution** — Cannot verify cargo-zigbuild cross-compile, gh release create, artifact upload, or that `host` correctly waits on `custom-release-smoke` failure without an actual tag push (the static wiring is correct).
3. **`cargo binstall bootroom` auto-discovery against a real GH Release** — Cannot verify without a real release.
4. **DIST-05 path-independence test under Docker after `make qemu-assets`** — Verified locally that the binary runs from `/tmp` (`overall: pass`); the test's degraded-sentinel assertion will only succeed under a tag-push with a real (non-`unknown`) committed rev.

### Gaps Summary

**No structural gaps requiring a closure plan.** The in-repo wiring for Phase 6 is complete and the four CRITICAL + five WARNING review findings are all confirmed fixed (CR-01 workspace dep version; CR-02 shell-glob pinning; CR-03 doctor contract assertion; CR-04 host.needs gate; WR-01 README pre-1.0 note; WR-02 actions/upload-artifact@v4; WR-03 LICENSE-APACHE copyright; WR-04 deny.toml v2 + yanked deny; WR-05 cargo package --no-verify removed).

The DIST-03 publish-step omission is the single material gap, but per 06-03 SUMMARY (Deviation #3) and the README pre-1.0 caveat, this is **accepted scope** — it is intentionally deferred to a follow-up plan and operationally bridged by the maintainer's first manual `cargo publish`. The README documents the workaround. The goal "kernel project on any supported platform installs `bootroom` in one step" therefore reaches the green state only after the first manual publish + tag push completes; until then, only `cargo install --git ...` works.

Because the remaining verification depends on real tag-push CI execution and a maintainer-driven first publish — both outside the verifier's reach — status is **human_needed** rather than passed or gaps_found.

---

_Verified: 2026-05-19T22:15:00Z_
_Verifier: Claude (gsd-verifier)_
