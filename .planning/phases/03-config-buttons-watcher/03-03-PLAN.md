---
phase: 03-config-buttons-watcher
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/bootroom/src/cli.rs
  - crates/bootroom/src/main.rs
  - crates/bootroom/src/lib.rs
autonomous: true
requirements: [CFG-01, CFG-07, CFG-08, ACT-03]
must_haves:
  truths:
    - "`bootroom --help` lists three subcommands: serve, check, init."
    - "`bootroom serve --help` lists every Phase-2 flag (`--kernel`, `--host`, `--port`, `--assets-dir`, `--no-open`) PLUS the new `--config` and `--action` flags."
    - "`bootroom serve --kernel /tmp/x --action 'reboot=reboot\\r' --action 'ctrlc=\\x03' --no-open` parses to ServeArgs whose `actions: Vec<CliAction>` has 2 entries with correctly decoded bytes (the unit test asserts this without binding a server)."
    - "`bootroom check` and `bootroom init` subcommands parse but DISPATCH to `unimplemented!()` stubs — Plan 04 wires them. The CLI surface lands here so subsequent plans can `match cli.cmd` exhaustively."
    - "Existing Phase-2 subprocess test `tests/serve_no_open.rs` still passes — `Cmd::Serve(ServeArgs)` retained as the FIRST variant (Pitfall #9 mitigation)."
  artifacts:
    - path: "crates/bootroom/src/cli.rs"
      provides: "Cmd enum (Serve/Check/Init), ServeArgs (extended), CheckArgs, InitArgs, CliAction re-export, parse_cli_action value parser"
      min_lines: 90
    - path: "crates/bootroom/src/main.rs"
      provides: "exhaustive match on Cmd::{Serve, Check, Init}"
      contains: "Cmd::Check"
    - path: "crates/bootroom/src/lib.rs"
      provides: "re-exports for Cmd, CheckArgs, InitArgs, parse_cli_action"
      contains: "CheckArgs"
  key_links:
    - from: "crates/bootroom/src/cli.rs"
      to: "crates/bootroom-core/src/escape.rs"
      via: "parse_cli_action calls bootroom_core::decode_bytes_escape"
      pattern: "decode_bytes_escape"
    - from: "crates/bootroom/src/main.rs"
      to: "crates/bootroom/src/cli.rs"
      via: "Cmd dispatch"
      pattern: "match cli.cmd"
---

<objective>
Refactor the CLI from a single-arm `Cmd::Serve(ServeArgs)` enum to three-arm `Cmd::{Serve, Check, Init}` with extended `ServeArgs` carrying `--config` and `--action`. `check` and `init` dispatch to stubs that Plan 04 will replace; serve is otherwise unchanged.

Purpose: CLI surface must land BEFORE the body wiring (Plans 04, 06, 07) so subsequent plans can match on the dispatch arms exhaustively without further CLI churn. Keeping `Cmd::Serve(ServeArgs)` first preserves the Phase-2 subprocess test invocation shape (Pitfall #9).

Output: `bootroom --help` shows three subcommands. `bootroom serve --help` shows `--config <PATH>` and `--action <LABEL=BYTES>`. `--action` is repeatable and decodes via the shared escape helper. The Phase-2 `serve_no_open.rs` integration test still passes unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-config-buttons-watcher/03-CONTEXT.md
@.planning/phases/03-config-buttons-watcher/03-RESEARCH.md
@.planning/phases/03-config-buttons-watcher/03-01-PLAN.md
@crates/bootroom/src/cli.rs
@crates/bootroom/src/main.rs
@crates/bootroom/src/lib.rs
@crates/bootroom/tests/serve_no_open.rs

<interfaces>
<!-- Re-exports plans 04 + downstream code rely on -->
From crates/bootroom/src/cli.rs (current):
```rust
pub enum Cmd { Serve(ServeArgs) }
pub struct ServeArgs { kernel: PathBuf, host: String, port: u16, assets_dir: Option<PathBuf>, no_open: bool }
```

<!-- New surface this plan adds -->
pub enum Cmd { Serve(ServeArgs), Check(CheckArgs), Init(InitArgs) }
pub struct ServeArgs { /* ... existing ... */, config: Option<PathBuf>, actions: Vec<CliAction> }
pub struct CheckArgs { config: Option<PathBuf> }
pub struct InitArgs { force: bool }
fn parse_cli_action(s: &str) -> Result<CliAction, String>

<!-- From bootroom-core (Plan 01) — already landed -->
pub use bootroom_core::config::CliAction;  // { pub label: String, pub bytes: Vec<u8> }
pub use bootroom_core::decode_bytes_escape;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend `cli.rs` — Cmd enum + ServeArgs/CheckArgs/InitArgs + parse_cli_action</name>
  <files>crates/bootroom/src/cli.rs</files>
  <behavior>
    - Existing `ServeArgs` fields and their clap attributes (`--kernel`, `--host`, `--port`, `--assets-dir`, `--no-open`) unchanged — same names, same types, same defaults, same help strings.
    - New `ServeArgs.config: Option<PathBuf>` with `#[arg(long, value_name = "PATH")]` and a one-line `Path to bootroom.toml; default = ./bootroom.toml.` help.
    - New `ServeArgs.actions: Vec<CliAction>` with `#[arg(long = "action", value_name = "LABEL=BYTES", action = clap::ArgAction::Append, value_parser = parse_cli_action)]` and help: `Define an ad-hoc action without editing config. Format: 'label=BYTES' with C-style escapes (\\r \\n \\t \\0 \\\\ \\xNN). Repeatable. Overrides config-file actions on label collision; last --action wins among repeated CLI values.`
    - `pub enum Cmd { Serve(ServeArgs), Check(CheckArgs), Init(InitArgs) }` — `Serve` MUST be the FIRST variant (Pitfall #9 — preserves help-text ordering and the Phase-2 subprocess test invocation shape).
    - `pub struct CheckArgs { #[arg(long, value_name = "PATH")] pub config: Option<PathBuf> }` with about-text: `Parse and validate bootroom.toml without starting the server.`
    - `pub struct InitArgs { #[arg(long)] pub force: bool }` with help on force: `Overwrite an existing bootroom.toml.` Subcommand about: `Write a starter bootroom.toml to the current directory.`
    - `fn parse_cli_action(s: &str) -> Result<CliAction, String>` per RESEARCH Pattern 4 (clap integration): splits on FIRST `=`, rejects empty label, calls `bootroom_core::decode_bytes_escape` on the rhs, wraps `EscapeError` into a String (`--action {label}: {e}`).
    - Unit tests (inline `#[cfg(test)] mod tests`):
      - `parse_cli_action_simple` — `"reboot=reboot\\r"` → `CliAction { label: "reboot", bytes: vec![b'r', b'e', b'b', b'o', b'o', b't', 0x0d] }`.
      - `parse_cli_action_hex` — `"ctrlc=\\x03"` → bytes `vec![0x03]`.
      - `parse_cli_action_empty_label_rejected` — `"=foo"` → `Err` containing "empty label".
      - `parse_cli_action_no_equals_rejected` — `"reboot"` → `Err` containing "expected 'label=BYTES'".
      - `parse_cli_action_invalid_escape_propagates` — `"x=\\q"` → `Err` containing "unknown escape" or the label "x".
      - `cli_parses_serve_with_repeated_actions` — uses `Cli::try_parse_from(["bootroom","serve","--kernel","/tmp/x","--no-open","--action","reboot=reboot\\r","--action","ctrlc=\\x03"])`; asserts `matches!(cli.cmd, Cmd::Serve(_))`; destructures and asserts `args.actions.len() == 2` and bytes match.
      - `cli_parses_check_with_config` — `Cli::try_parse_from(["bootroom","check","--config","/tmp/bootroom.toml"])` → `Cmd::Check(CheckArgs { config: Some(_) })`.
      - `cli_parses_init_force` — `Cli::try_parse_from(["bootroom","init","--force"])` → `Cmd::Init(InitArgs { force: true })`.
      - `cli_parses_init_default` — `Cli::try_parse_from(["bootroom","init"])` → `Cmd::Init(InitArgs { force: false })`.
      - `cli_serve_args_phase2_compat` — `Cli::try_parse_from(["bootroom","serve","--kernel","/tmp/x","--host","::1","--port","9999","--no-open"])` succeeds; assert all four Phase-2 fields populated correctly and `args.config.is_none()` and `args.actions.is_empty()`.
  </behavior>
  <action>
    Rewrite `crates/bootroom/src/cli.rs` to:

    1. Keep the existing `Cli` struct (no change — just `#[command(subcommand)] pub cmd: Cmd`).
    2. Extend `Cmd` to three variants in this order: `Serve(ServeArgs)`, `Check(CheckArgs)`, `Init(InitArgs)`. Per-variant doc comments map to the about-text shown in `--help`.
    3. Extend `ServeArgs` with two new fields per the behavior block; do NOT touch the five existing fields. The existing `#[derive(Debug, Args, Clone)]` on `ServeArgs` stays — `CliAction` must be `Clone + Debug` (Plan 01 already derives both).
    4. Add `CheckArgs` and `InitArgs` as `#[derive(Debug, Args, Clone)]`.
    5. Add `fn parse_cli_action(s: &str) -> Result<CliAction, String>` as a private module function.
    6. Add `use bootroom_core::config::CliAction;` and `use bootroom_core::decode_bytes_escape;` at the top.
    7. Inline `#[cfg(test)] mod tests` with the 10 named tests above.

    Per RESEARCH Pattern 4: the value-parser closure must NOT panic on malformed input — every error path returns `Err(String)` which clap renders as the usage error.

    Per CONTEXT D-02: clap `ArgAction::Append` makes `--action` repeatable. The combined override semantics (CLI overrides TOML; last `--action label=X` wins among repeats) are enforced in `LoadedConfig::load_from_str_with_overrides` (already implemented in Plan 01) — this plan's `Vec<CliAction>` is just the source-of-truth list passed to that function. Do NOT dedupe in `parse_cli_action`; let `from_config`'s merge handle it (Plan 01 tests `last_cli_action_wins_for_same_label`).

    Do NOT inline a CLI builder pattern — `derive` is what the existing code uses (Plan 1's `RESEARCH State of the Art` line).
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/norn-web && cargo test -p bootroom --lib cli::tests 2>&1 | tail -20</automated>
  </verify>
  <done>10 named cli::tests pass. `cargo clippy -p bootroom --lib -- -D warnings` clean. Existing `ServeArgs` Phase-2 field shape preserved (verified by `cli_serve_args_phase2_compat`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update main.rs dispatch + lib.rs re-exports + scope helpers</name>
  <files>crates/bootroom/src/main.rs, crates/bootroom/src/lib.rs</files>
  <behavior>
    - `cargo build -p bootroom` succeeds.
    - `cargo run -p bootroom -- --help` (or equivalent under test harness) lists three subcommands: `serve`, `check`, `init`. (Verified via integration test below.)
    - `cargo run -p bootroom -- check` exits with a non-zero status and an `unimplemented` message (this is the temporary Plan-03 state; Plan 04 replaces). Integration test asserts the exit is non-success — the EXACT exit code/message changes in Plan 04 and is intentionally not asserted here.
    - `cargo run -p bootroom -- init` same.
    - `cargo test --test serve_no_open` — existing Phase-2 integration test passes UNCHANGED (Pitfall #9 confirmation).
  </behavior>
  <action>
    Step 1: Edit `crates/bootroom/src/main.rs` `match cli.cmd` to be exhaustive over the new three-arm enum:

    - `Cmd::Serve(args) => bootroom::server::run(args).await` — UNCHANGED.
    - `Cmd::Check(args) => { let _ = args; std::process::exit(2); }` — placeholder. Plan 04 wires `bootroom::check_cmd::run(args)`. Pick `2` (file-not-found-class) so any accidental CI use during the Plan 03 → Plan 04 window fails loudly.
    - `Cmd::Init(args) => { let _ = args; std::process::exit(1); }` — placeholder. Plan 04 wires `bootroom::init_cmd::run(args)`.

    Add a `// Plan 04 wires real handlers; placeholder until then.` comment on each placeholder arm so reviewers don't think this is final.

    Step 2: Edit `crates/bootroom/src/lib.rs` re-exports:

    - Existing: `pub use cli::ServeArgs;` — keep.
    - Add: `pub use cli::{Cli, Cmd, CheckArgs, InitArgs};` (Cli/Cmd are needed by Plan 04's check/init handlers' test harnesses).

    Step 3: Create a NEW integration test file `crates/bootroom/tests/cli_subcommands.rs` (Wave 0 contribution — covers CFG-07/CFG-08 scaffolding before Plan 04's real implementation):

    - `cli_help_lists_three_subcommands` — spawn `Command::new(env!("CARGO_BIN_EXE_bootroom")).arg("--help")`; capture stdout; assert it contains `"serve"`, `"check"`, `"init"`.
    - `cli_serve_help_includes_config_and_action` — `bootroom serve --help` stdout contains `"--config"` and `"--action"` and `"LABEL=BYTES"`.
    - `cli_check_help_includes_config` — `bootroom check --help` stdout contains `"--config"`.
    - `cli_init_help_includes_force` — `bootroom init --help` stdout contains `"--force"`.
    - `cli_check_stub_exits_nonzero` — `bootroom check` runs; status is NOT success. (Plan 04 will rewrite this test with real exit codes 0/1/2/3.)
    - `cli_init_stub_exits_nonzero` — `bootroom init` runs; status is NOT success.

    Tests use the existing `CARGO_BIN_EXE_bootroom` env var (Cargo sets it for integration tests). No tokio-runtime needed for help-only subprocess tests; use `std::process::Command`.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/norn-web && cargo test --test cli_subcommands 2>&1 | tail -15 && cargo test --test serve_no_open 2>&1 | tail -10 && cargo clippy -p bootroom --tests -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>6 cli_subcommands tests pass. The pre-existing `serve_no_open` test still passes (Pitfall #9 confirmed). `cargo clippy -p bootroom --tests -- -D warnings` clean. `--help` for all three subcommands renders correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv → bootroom | Operator-controlled CLI args cross from shell into clap; this plan extends the parser. |
| (no network surface) | Pure CLI parsing — no I/O until Plan 04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | `parse_cli_action` value parser | mitigate | Returns `Result<_, String>` for every malformed input; clap renders the error and exits 2 (clap convention). No panic path. Tested across 5 error/success cases. |
| T-03-03-02 | Confused deputy | `--action 'label=BYTES'` decoding | mitigate | Splits on FIRST `=` (operator may want `=` inside bytes, e.g. `key=val\\r`); rejects empty label. `decode_bytes_escape` is the shared canonical decoder per Plan 01 — no second escape grammar to drift from the TOML one. |
| T-03-03-03 | DoS (resource) | `--action` repeatable | accept | bootroom is operator-driven; the same operator typing 10,000 `--action` flags is choosing their own resource ceiling. No mitigation needed. |
| T-03-03-04 | Tampering (regression) | Phase-2 ServeArgs surface | mitigate | `cli_serve_args_phase2_compat` test pins all five Phase-2 fields; existing `tests/serve_no_open.rs` integration test passes unchanged (Pitfall #9 confirmation). |
</threat_model>

<verification>
- `cargo test -p bootroom --lib cli::tests` — 10 tests pass.
- `cargo test --test cli_subcommands` — 6 tests pass.
- `cargo test --test serve_no_open` — Phase-2 test still passes UNCHANGED.
- `cargo clippy -p bootroom --lib --tests -- -D warnings` exits 0.
- `bootroom --help` shows three subcommands; `bootroom serve --help` shows `--config` and `--action`.
</verification>

<success_criteria>
- CLI surface stable for Plans 04, 06, 07: `Cmd::{Serve, Check, Init}` exhaustive; `ServeArgs.config` and `ServeArgs.actions` available; placeholder dispatch in main.rs.
- ACT-03 CLI half landed: `--action` flag parses, decodes, repeats; the merge semantics live in `LoadedConfig` (Plan 01).
- CFG-01 prerequisite landed: `--config <PATH>` parseable on `serve` and `check`; default behavior (CWD `bootroom.toml`) realized in Plan 04 `LoadedConfig::load` callers.
- CFG-07 and CFG-08 stub subcommands wired; Plan 04 replaces stubs with real handlers.
- Pitfall #9 (subprocess test regression) avoided — Phase-2 `serve_no_open.rs` still green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-config-buttons-watcher/03-03-SUMMARY.md` covering: new `Cmd` enum shape, extended `ServeArgs` fields with their clap attribute strings, `parse_cli_action` behavior summary, the new `tests/cli_subcommands.rs` test list, explicit note that `cli_check_stub_exits_nonzero` and `cli_init_stub_exits_nonzero` will be rewritten by Plan 04 with real exit-code assertions, and confirmation that `serve_no_open.rs` still passes (Pitfall #9 cleared).
</output>
