---
phase: 03-config-buttons-watcher
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bootroom/web/index.html
  - crates/bootroom/web/style.css
autonomous: true
requirements: [ACT-01, ACT-04, CFG-10, WCH-05]
must_haves:
  truths:
    - "`index.html` contains `<div id=\"actions-panel\" hidden>` between `#hdr` and `#terminal` (after #iso-banner placement)."
    - "`index.html` contains `<div id=\"fresh-banner\" hidden>` (empty container; populated by app.js per WS frame)."
    - "`index.html` contains `<div id=\"config-banner\" role=\"alert\" hidden>` (empty container; populated by app.js)."
    - "`style.css` adds rules for `#actions-panel`, `.action-group`, `.action-group-label`, `.action-btn`, `#fresh-banner`, `#banner-launch`, `#banner-dismiss`, `#config-banner`, and `.pill[data-state=\"BUSY\"]` using ONLY existing `:root` tokens (zero new hex values outside `:root`)."
    - "`node --check` on app.js and funnel.js still passes (no JS edits in this plan)."
    - "All Phase-2 styles preserved (no rule deletions); the Phase-2 button selector-list is EXTENDED in place to include `.action-btn`."
  artifacts:
    - path: "crates/bootroom/web/index.html"
      provides: "#actions-panel + #fresh-banner + #config-banner DOM (initial hidden state)"
      contains: "actions-panel"
    - path: "crates/bootroom/web/style.css"
      provides: "Phase-3 CSS additions per UI-SPEC line 232-318"
      contains: "data-state=\"BUSY\""
  key_links:
    - from: "crates/bootroom/web/index.html"
      to: "crates/bootroom/web/style.css"
      via: "id and class selectors"
      pattern: "#actions-panel"
    - from: "crates/bootroom/web/index.html"
      to: "crates/bootroom/web/app.js (Plan 11)"
      via: "document.getElementById lookups"
      pattern: "getElementById"
---

<objective>
Add the three new DOM containers (`#actions-panel`, `#fresh-banner`, `#config-banner`) and the supporting CSS. Pure markup + styling; no JS behavior wired in this plan.

Purpose: Land the DOM + CSS surface FIRST so Plans 10 (funnel lock API) and 11 (app.js wiring) have selectors to attach to. UI-SPEC explicit: zero new hex values outside `:root`, zero new tokens.

Output: Updated `index.html` with three new hidden containers; updated `style.css` with the Phase-3 rules. No JS changes; existing `node --check` still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-config-buttons-watcher/03-CONTEXT.md
@.planning/phases/03-config-buttons-watcher/03-UI-SPEC.md
@crates/bootroom/web/index.html
@crates/bootroom/web/style.css

<interfaces>
DOM vertical placement order (UI-SPEC Component Inventory):
  #hdr (existing)
  #iso-banner (existing — Phase 1)
  #actions-panel (NEW — empty container, [hidden])
  #fresh-banner (NEW — empty container, [hidden])
  #config-banner (NEW — empty container, [hidden], role="alert")
  #terminal (existing)

Children of #actions-panel (populated by Plan 11 renderActionButtons):
  Zero-or-more `.action-group` divs; each contains:
    <span class="action-group-label">BOOT</span>
    <button class="action-btn" data-action-label="reboot" data-bytes-b64="...">REBOOT</button>

Children of #fresh-banner (populated by Plan 11 KernelChanged handler):
  Success variant: span.banner-text + #banner-launch + #banner-dismiss
  Warning variant: span.banner-text + #banner-dismiss only (no launch)

Children of #config-banner (populated by Plan 11 ConfigInvalid handler):
  <strong>bootroom.toml is invalid</strong>
  <p class="err-body">…error message…</p>

CSS tokens available (from existing :root):
  --xs --sm --md --lg
  --bg --surface --accent --fg --fg-muted
  --status-loading --status-running --status-halted
  --banner-bg --banner-head
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add three new DOM containers to `index.html`</name>
  <files>crates/bootroom/web/index.html</files>
  <behavior>
    - After the existing `#iso-banner` closing `</div>` (currently line 46) and BEFORE `<div id="terminal">` (currently line 48), three new top-level divs appear in this order:
      1. `<div id="actions-panel" hidden></div>`
      2. `<div id="fresh-banner" hidden></div>`
      3. `<div id="config-banner" role="alert" hidden></div>`
    - Each div is initially empty; Plan 11 populates children at runtime.
    - All three have the `hidden` attribute (HTML5 default-hidden; Phase-1 style.css's `[hidden] { display: none; }` rule covers them).
    - No edits to `<header id="hdr">`, `#iso-banner`, `<div id="terminal">`, or any of the script tags.
    - `node --check crates/bootroom/web/app.js` and `node --check crates/bootroom/web/funnel.js` still pass.
    - The existing `tests/embedded_assets_served.rs` test still passes (include_dir picks up the new HTML automatically).
  </behavior>
  <action>
    Edit `crates/bootroom/web/index.html` to insert the three new divs in the slot identified above. Use HTML comments to make them grep-friendly during execution and review:

    - `<!-- Phase 3: action buttons panel; populated by app.js renderActionButtons. -->` above the `#actions-panel` div.
    - `<!-- Phase 3: fresh-kernel banner; populated by app.js KernelChanged handler. -->` above the `#fresh-banner` div.
    - `<!-- Phase 3: config-invalid banner; populated by app.js ConfigInvalid handler. -->` above the `#config-banner` div.

    Match the Phase-2 commenting style. Containers are empty (no children); JS owns the inner DOM construction so the HTML stays minimal and identical across success/warning banner variants.

    Do NOT change any existing line. Do NOT touch the inline SAB-probe script. Do NOT touch the script tags at the bottom.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/norn-web && grep -c 'id="actions-panel"' crates/bootroom/web/index.html && grep -c 'id="fresh-banner"' crates/bootroom/web/index.html && grep -c 'id="config-banner"' crates/bootroom/web/index.html && node --check crates/bootroom/web/app.js && node --check crates/bootroom/web/funnel.js && cargo test --test embedded_assets_served 2>&1 | tail -5</automated>
  </verify>
  <done>Three `grep -c` outputs are each `1`. Both `node --check` calls clean. `embedded_assets_served` test still green. DOM ready for Plan 11 to populate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase-3 CSS to `style.css` (zero new hex outside :root)</name>
  <files>crates/bootroom/web/style.css</files>
  <behavior>
    - `style.css` gains a new `/* Phase 3 additions */` section at the END (after the existing IDLE pill rule, around current line 238).
    - The Phase-2 button selector list at line 194 (`.hdr-btns button, .term-ctrls button {`) is EXTENDED in place to `.hdr-btns button, .term-ctrls button, .action-btn {`. The companion `:hover`, `:focus-visible`, `:disabled` rules at lines 209-224 get the same one-token addition. This reuses Phase-2 button styling for action buttons per UI-SPEC line 263 (executor freedom; this plan picks the in-place extension as the smallest diff).
    - New rules at the bottom of the file cover: `#actions-panel`, `.action-group`, `.action-group-label`, `#fresh-banner`, `#fresh-banner .banner-text`, `#banner-launch`, `#banner-dismiss`, `#config-banner`, `#config-banner strong`, `#config-banner .err-body`, `.pill[data-state="BUSY"]`.
    - All color references use `var(--token)`; no new hex value appears outside the existing `:root` block.
    - All spacing references use `var(--xs|sm|md|lg)`; no new px values for padding/gap (UI-SPEC `max-height: 25vh` on `#actions-panel` is a layout constraint, not a token).
    - `.pill[data-state="BUSY"]` is byte-for-byte identical to the existing `.pill[data-state="IDLE"]` rule per UI-SPEC line 95 (the literal pill text is the discriminator).
  </behavior>
  <action>
    Edit `crates/bootroom/web/style.css`:

    Step 1: Extend the existing Phase-2 button selector lists in place:
      - Line ~194: change `.hdr-btns button, .term-ctrls button {` to `.hdr-btns button, .term-ctrls button, .action-btn {`.
      - Line ~209 (`:hover` rule): same one-token extension.
      - Line ~214 (`:focus-visible` rule): same.
      - Line ~220 (`:disabled` rule): same.

    Step 2: Append a new section at end-of-file, separated by a comment band matching the existing `/* ------... */` style. Section header: `/* Phase 3 additions: actions panel, banners, BUSY pill */`.

    Step 3: Add the rules. Each rule's property/value pairs as listed (UI-SPEC line 236-318 is the authoritative source):

    - `#actions-panel` — `display: flex; flex-wrap: wrap; gap: var(--md); align-items: center; padding: var(--sm) var(--md); background: var(--surface); max-height: 25vh; overflow-y: auto;`
    - `.action-group` — `display: flex; align-items: center; gap: var(--sm);`
    - `.action-group-label` — `color: var(--fg-muted); font-size: 12px; font-weight: 600; line-height: 1.4; text-transform: uppercase; letter-spacing: 0.05em;`
    - `#fresh-banner` — `display: flex; align-items: center; gap: var(--sm); padding: var(--sm) var(--md); background: var(--surface);`
    - `#fresh-banner .banner-text` — `color: var(--fg); font-size: 12px; font-weight: 600; line-height: 1.4;`
    - `#banner-launch` — `color: var(--accent);` (overrides the inherited `--fg` from the extended .action-btn rule; the Phase-2 cascade keeps the focus-visible outline.)
    - `#banner-dismiss` — `color: var(--fg-muted); padding: var(--xs) var(--sm);`
    - `#config-banner` — `background: var(--banner-bg); padding: var(--md);`
    - `#config-banner strong` — `display: block; margin: 0 0 var(--xs) 0; color: var(--banner-head); font-size: 12px; font-weight: 600; line-height: 1.4;`
    - `#config-banner .err-body` — `margin: 0; color: var(--fg); font-size: 13px; font-weight: 400; line-height: 1.5;`
    - `.pill[data-state="BUSY"]` — `background: var(--fg-muted); color: var(--fg);`

    Step 4: Do NOT touch the `:root` block. Do NOT introduce any new hex value anywhere outside `:root`.

    Step 5: After writing the file, run a grep to confirm no new hex outside `:root`. The shell command to use (single line, copy verbatim): `node -e "const s=require('fs').readFileSync('crates/bootroom/web/style.css','utf8').replace(/:root\s*\{[\s\S]*?\}/,'').replace(/\/\*[\s\S]*?\*\//g,''); const m=s.match(/#[0-9a-fA-F]{6}/g)||[]; console.log(m.length===0?'OK':'FAIL: '+m.join(','))"`. Output must be `OK`.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/norn-web && grep -q 'data-state="BUSY"' crates/bootroom/web/style.css && grep -q '#actions-panel' crates/bootroom/web/style.css && grep -q '#fresh-banner' crates/bootroom/web/style.css && grep -q '#config-banner' crates/bootroom/web/style.css && grep -q ', .action-btn {' crates/bootroom/web/style.css && node -e "const s=require('fs').readFileSync('crates/bootroom/web/style.css','utf8').replace(/:root\s*\{[\s\S]*?\}/,'').replace(/\/\*[\s\S]*?\*\//g,''); const m=s.match(/#[0-9a-fA-F]{6}/g)||[]; if(m.length!==0){console.error('FAIL hex outside root: '+m.join(','));process.exit(1)}console.log('OK')" && cargo test --test embedded_assets_served 2>&1 | tail -3</automated>
  </verify>
  <done>All selector greps green; node-script gate confirms zero hex outside `:root`; `.action-btn` joined the button rule via in-place selector extension; embedded_assets test still green. CSS surface ready for Plan 10 (lock indicator on pill) + Plan 11 (banner state machine + button rendering).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new server boundary) | Pure web-asset edits; existing COOP/COEP and asset-serving paths unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-01 | Tampering (visual deception) | Banner content injected by Plan 11 | mitigate | Plan 11's `ConfigInvalid` body uses `textContent` (NOT `innerHTML`); the `error` field from `WsMessage::ConfigInvalid` never reaches the DOM as HTML. This plan's CSS does not expose injection vectors; the only attributes used are id/class/role. |
| T-03-09-02 | Information disclosure | role="alert" announcing config errors aloud | accept | Operator's own TOML error content; same as RESEARCH Pitfall #5/T-03-04-04 disposition. |
| T-03-09-03 | UI palette drift | accidental new hex value | mitigate | Node-script grep gate in `<verify>` rejects any hex outside `:root`. Executor cannot ship a palette regression. |
</threat_model>

<verification>
- `grep` gates for the three new IDs all return 1.
- `node --check` on `app.js` and `funnel.js` clean.
- Node-script grep gate confirms zero hex outside `:root` block.
- `cargo test --test embedded_assets_served` still passes (build.rs re-embeds the updated files).
- Phase-2 button styling extended in place to include `.action-btn`; no rule deletions.
</verification>

<success_criteria>
- DOM surface ready for Plan 11: three hidden containers in the correct vertical slot.
- CSS surface ready: action buttons inherit Phase-2 styling via the in-place selector-list extension; banners and BUSY pill use existing tokens only.
- UI-SPEC palette and spacing rules respected — zero new hex, zero new tokens, executor-grep-verified.
- Plans 10 + 11 can proceed in parallel knowing the DOM IDs and CSS selectors exist.
</success_criteria>

<output>
After completion, create `.planning/phases/03-config-buttons-watcher/03-09-SUMMARY.md` listing: the three new HTML IDs (with line numbers post-edit), the in-place selector-list extension (one-line edit at lines 194/209/214/220), the new CSS rules added, the grep-gate result (`OK`), and confirmation that `node --check` and `embedded_assets_served` still pass.
</output>
