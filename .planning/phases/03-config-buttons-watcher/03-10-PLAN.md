---
phase: 03-config-buttons-watcher
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bootroom/web/funnel.js
autonomous: true
requirements: [ACT-04]
must_haves:
  truths:
    - "`Funnel` instances expose `lockInput()` and `unlockInput()` methods; both are idempotent."
    - "`Funnel` instances have a public `locked: boolean` flag, default `false`."
    - "`setLockObserver(cb)` registers a single observer that fires with the boolean `locked` value on every `lockInput`/`unlockInput` call."
    - "`enqueue` is UNCHANGED — bytes flow regardless of `locked` (the lock is enforced at the CALLER per CONTEXT.md `<decisions>` Funnel input lock primitive). Plan 11 enforces caller-side guards in `xterm.onData` and action-button click handlers."
    - "`node --check crates/bootroom/web/funnel.js` passes."
    - "The Phase-2 funnel manual-test-plan in the file's comment block is updated to add a Phase-3 lock/unlock test case (the DevTools manual test referenced by UI-SPEC Interaction Contract 9)."
  artifacts:
    - path: "crates/bootroom/web/funnel.js"
      provides: "Funnel.lockInput/unlockInput methods + locked flag + setLockObserver export"
      contains: "lockInput"
  key_links:
    - from: "crates/bootroom/web/funnel.js (Funnel class)"
      to: "crates/bootroom/web/app.js (Plan 11)"
      via: "setLockObserver callback drives pill + button-disabled DOM mutations"
      pattern: "setLockObserver"
---

<objective>
Extend the `Funnel` class with a `locked` flag plus `lockInput()`/`unlockInput()` methods. Add a module-level `setLockObserver(cb)` export so app.js (Plan 11) can observe lock state changes without polling.

Purpose: Ships the API surface Phase 4's scenario engine will call. Phase 3 ships it UNUSED inside the funnel itself (enqueue does NOT short-circuit — that decision in CONTEXT.md `<decisions>` "Funnel input lock primitive" — caller-side enforcement keeps server-initiated `SerialIn` flowing during the lock).

Output: Updated `funnel.js` with the additions; manual test plan in the file comment block extended with a lock/unlock case. No new exports beyond `setLockObserver` (the `lockInput`/`unlockInput` are instance methods on the existing exported `Funnel` class).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-config-buttons-watcher/03-CONTEXT.md
@.planning/phases/03-config-buttons-watcher/03-RESEARCH.md
@.planning/phases/03-config-buttons-watcher/03-UI-SPEC.md
@crates/bootroom/web/funnel.js

<interfaces>
<!-- Current Funnel class (Phase 2) -->
export class Funnel {
  constructor(slave, ldisc) {
    this.slave = slave;
    this.ldisc = ldisc;
    this.queue = [];
    this.draining = false;
  }
  enqueue(bytes, opts = {}) { /* ... */ }
  async #drain() { /* ... */ }
}

<!-- Extensions this plan adds -->
- this.locked = false;  // in constructor
- lockInput()   — set this.locked = true,  invoke observer with true.  Idempotent.
- unlockInput() — set this.locked = false, invoke observer with false. Idempotent.
- export function setLockObserver(cb)  — registers a module-level observer fn.
- enqueue() and #drain() UNCHANGED.

<!-- Why caller-side enforcement (CONTEXT.md Funnel input lock primitive) -->
Server-initiated WsMessage::SerialIn must still reach the guest during a scenario lock (the scenario engine in Phase 4 IS the WS sender doing the locking). If enqueue short-circuited on this.locked, the scenario engine's own writes would be self-blocked.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lockInput/unlockInput + locked flag + setLockObserver export</name>
  <files>crates/bootroom/web/funnel.js</files>
  <behavior>
    - `new Funnel(slave, ldisc).locked === false` initially.
    - `funnel.lockInput()` sets `funnel.locked = true` and invokes the registered observer with `true`. Calling it again while locked is a no-op (idempotent — does NOT fire the observer a second time).
    - `funnel.unlockInput()` sets `funnel.locked = false` and invokes the observer with `false`. Idempotent — no-op when already unlocked.
    - `setLockObserver(cb)` replaces the previous observer (only one observer at a time; app.js is the sole expected consumer).
    - `setLockObserver` is exported alongside the existing `Funnel`, `bytesToB64`, `b64ToBytes`, `keyEventToBytes` exports.
    - `enqueue()` BODY UNCHANGED. The `#drain` loop UNCHANGED. The lock has zero effect on byte flow inside funnel.js itself.
    - The file's existing module doc comment is updated to add a paragraph explaining the lock primitive's CONTEXT-locked semantics (caller-side enforcement; ships unused in Phase 3; Phase 4 first caller).
    - The file's existing "MANUAL TEST PLAN" comment block gains a 6th test case for the lock/unlock behavior (per UI-SPEC Interaction Contract 9).
    - `node --check crates/bootroom/web/funnel.js` passes.
    - No new dependencies (no imports added).
    - Plan-11-side guards (xterm.onData and action-button click) are NOT this plan's responsibility — they land in Plan 11.
  </behavior>
  <action>
    Edit `crates/bootroom/web/funnel.js`:

    Step 1: Constructor — add `this.locked = false;` at the end of the existing constructor body (after `this.draining = false;`). Add a brief JSDoc note above the field referencing CONTEXT.md `<decisions>` "Funnel input lock primitive".

    Step 2: Add two instance methods to the `Funnel` class, immediately after `enqueue` (before `#drain` to keep the public API contiguous):

    - `lockInput()`:
        - If `this.locked === true`, return (idempotent — observer NOT fired on no-op).
        - Else: `this.locked = true; _notifyLockObserver(true);`.

    - `unlockInput()`:
        - If `this.locked === false`, return.
        - Else: `this.locked = false; _notifyLockObserver(false);`.

    JSDoc each method with one-line summary + a reference to UI-SPEC Interaction Contract 9 (the manual DevTools test). Note the no-short-circuit semantics in the lockInput doc: "this.locked is observed by callers (xterm.onData, action-btn click handlers in app.js) which short-circuit before calling enqueue; enqueue itself is lock-agnostic so server-initiated SerialIn frames continue to flow during scenario execution."

    Step 3: Add a module-level observer registry and the `setLockObserver` export OUTSIDE the class (placed after the Funnel class, before `bytesToB64`):

    - `let _lockObserver = () => {};` (default no-op).
    - `export function setLockObserver(cb) { _lockObserver = typeof cb === 'function' ? cb : () => {}; }` — defensive: if a non-function is passed, fall back to no-op (avoids a thrown TypeError inside lockInput's call site).
    - `function _notifyLockObserver(value) { try { _lockObserver(value); } catch (e) { console.warn('[funnel] lock observer threw:', e); } }` — observer-throws must NOT prevent the lock state change from being effective (mirrors the WR-05 try/catch pattern around writeFromLower in #drain).

    Step 4: Extend the existing module doc comment at the top of the file. Add a paragraph (after the existing "Locked decision" paragraph) titled "Lock primitive (Phase 3 addition):" summarizing:
    - The lock ships unused in Phase 3; Phase 4's scenario engine is the first caller.
    - Why enqueue does NOT short-circuit on locked: server-initiated SerialIn (scenario steps) must continue to flow; otherwise the scenario engine self-blocks.
    - The lock is enforced at the CALLER (xterm.onData + button click handlers in app.js).
    - setLockObserver is the one-way feedback channel for app.js to update the status pill (`BUSY`) and the disabled state of `.action-btn` elements.

    Step 5: Extend the MANUAL TEST PLAN comment block at the bottom of the file. Add a 6th test case:

    `6. lockInput / unlockInput + observer (Phase 3, ACT-04):`
    - paste into DevTools console:
        - `setLockObserver(v => console.log('lock changed →', v));`
        - `funnel.lockInput();` → expect `lock changed → true` printed.
        - `funnel.lockInput();` → expect NO new log line (idempotent).
        - `funnel.locked;` → expect `true`.
        - `funnel.unlockInput();` → expect `lock changed → false`.
        - `funnel.unlockInput();` → no new log (idempotent).
        - `funnel.locked;` → expect `false`.
    - With Plan 11 wired: `funnel.lockInput()` flips the pill to `BUSY` and disables every `.action-btn`; `funnel.unlockInput()` restores the previous pill state and re-enables buttons.

    Do NOT change `enqueue`. Do NOT change `#drain`. Do NOT modify `bytesToB64`, `b64ToBytes`, or `keyEventToBytes`. The Phase-2 single-writer invariant + the WS-02 mitigation are preserved unchanged.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/norn-web && node --check crates/bootroom/web/funnel.js && grep -q 'lockInput' crates/bootroom/web/funnel.js && grep -q 'unlockInput' crates/bootroom/web/funnel.js && grep -q 'setLockObserver' crates/bootroom/web/funnel.js && grep -q 'this.locked = false' crates/bootroom/web/funnel.js && cargo test --test embedded_assets_served 2>&1 | tail -3</automated>
  </verify>
  <done>`node --check` clean. All four grep gates pass. embedded_assets test still green (include_dir re-embeds funnel.js). The lock API surface ships ready for Phase 4 consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary) | JS-only change in a loopback dev-tool's static asset. No new server surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-01 | DoS (observer-throws) | `_notifyLockObserver` | mitigate | try/catch around the observer invocation logs warn + continues (mirrors Phase 2 WR-05 `writeFromLower` try/catch). The lock state change still takes effect regardless. |
| T-03-10-02 | Confused deputy (observer hijack) | `setLockObserver` accepts any function | accept | bootroom is loopback-only and JS module scope is the only caller; no adversarial caller exists. Defense-in-depth: function-type check on input. |
| T-03-10-03 | Lock-bypass (enqueue ignores lock) | enqueue + caller-side enforcement | mitigate | Per CONTEXT.md `<decisions>` Funnel input lock primitive: enforcement at the CALLER is the locked design decision. Plan 11 implements the xterm.onData + button-click guards. If a Phase-4 caller writes new code paths that bypass those guards, the lock is silently bypassed — Plan 11 documents the guard sites; Phase 4 plans must respect them. |
| T-03-10-04 | Information disclosure (lock state) | this.locked is public | accept | Same disposition as Phase 2's "queue/draining are conventionally public" comment in the existing module doc — defense-in-depth only; no adversarial caller in scope. |
</threat_model>

<verification>
- `node --check crates/bootroom/web/funnel.js` exits 0.
- Four grep gates (`lockInput`, `unlockInput`, `setLockObserver`, `this.locked = false`) all match.
- `tests/embedded_assets_served.rs` still passes (re-embed picked up the change).
- No edits to enqueue / #drain / Phase-2 helpers.
- The manual-test-plan comment block has a 6th case.
</verification>

<success_criteria>
- ACT-04 surface satisfied at the funnel layer: `lockInput`/`unlockInput`/`setLockObserver` exist; `funnel.locked` is observable. Plan 11 wires the caller-side guards + the pill/buttons observer; the manual DevTools test (UI-SPEC Interaction Contract 9) becomes runnable after Plan 11 ships.
- Phase 4 scenario engine has the API surface it expects, with no need to re-architect when it ships.
- Phase 2 funnel byte-flow semantics preserved 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/03-config-buttons-watcher/03-10-SUMMARY.md` covering: the three additions (locked flag, lockInput/unlockInput, setLockObserver), the explicit no-short-circuit-in-enqueue rationale (with link to CONTEXT.md), the 6th manual test case added to the file, and a note that Plan 11 owns the caller-side guards and observer.
</output>
